use warnings;
use strict;

use Test::More tests => 1;

use Geo::OSM::Primitive::Node;
use Geo::OSM::Primitive::Way ;

# my $way = Geo::OSM::Primitive::Way->new(1);

# is($way->cnt_nodes(), '0 nodes in way');
ok(1);
